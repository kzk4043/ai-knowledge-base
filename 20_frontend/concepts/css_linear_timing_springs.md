# CSS Linear Timing Function - Springs & Bounces

## メタデータ  
- **URL**: https://www.joshwcomeau.com/animation/linear-timing-function/
- **日付**: 2025-10-31
- **重要度**: 🟡 中
- **タグ**: CSS Animation, linear() function, Spring Physics, Joshua Comeau

## 概要
CSS `linear()`タイミング関数により、JavaScriptなしでスプリング・バウンス物理アニメーションをネイティブCSSで実現可能に。複雑なアニメーションライブラリの代替手段。

## 新技術の特徴

### `linear()`関数の革新性
```css
.spring-animation {
  animation: move 1s linear(
    0, 0.006, 0.025, 0.057, 0.1, 0.154, 0.217, 0.289, 0.37, 0.458,
    0.552, 0.652, 0.757, 0.864, 0.973, 1.083, 1.192, 1.3, 1.405, 1.507,
    /* ...制御点の続き... */
  );
}
```

**従来との比較:**
- **従来**: `ease`, `ease-in-out`等の限定的なカーブ
- **新技術**: 数十の制御点による複雑な物理モーション

### 物理ベースアニメーションの実現

**スプリング物理:**
```css
.spring {
  animation: bounce 1.2s linear(
    0, 0.004, 0.016, 0.035, 0.063, 0.098, 0.141, 0.191, 0.248, 0.312,
    0.382, 0.457, 0.536, 0.618, 0.704, 0.791, 0.878, 0.963, 1.046, 1.125,
    1.199, 1.267, 1.328, 1.381, 1.426, 1.462, 1.488, 1.504, 1.509, 1.503,
    1.485, 1.456, 1.414, 1.361, 1.296, 1.219, 1.131, 1.031, 0.921, 0.8
  );
}
```

**バウンス効果:**
- 自然な跳ね返り動作
- 減衰振動の表現
- 物理法則に基づく動き

## 実装ツール

### Linear Easing Generator
- JavaScript関数からCSS `linear()`への変換
- SVGカーブの`linear()`対応
- プリセット: Spring, Bounce, Elastic, Material Design

**設定可能項目:**
- 制御点の数
- 精度レベル  
- カスタムイージング

### プリセット例
```css
/* Material Design Emphasized */
.material-emphasis {
  animation: slide 0.5s linear(
    0, 0.002, 0.01, 0.023, 0.041, 0.064, 0.092, 0.125, 0.162,
    /* ...続き */
  );
}
```

## パフォーマンス分析

### ベンチマーク結果
- **CSS vs JavaScript**: 同等のスムーズさ
- **ローエンド端末**: 性能差なし
- **バンドルサイズ**: 大規模スプリング3つで+1.3kB

### ネットワーク影響
- 3G接続（2mbps）: 5ms追加ダウンロード時間
- 実用的な影響は最小限

## 技術的制約

### 時間ベース制約
```javascript
// JavaScript（理想的）
const spring = useSpring({
  stiffness: 200,
  damping: 15
});

// CSS（制約あり）  
animation: move 1s linear(/* 事前計算済み制御点 */);
```

**課題:**
- 物理ベースでなく時間ベース
- 動的パラメータ変更不可
- 事前計算が必要

### 適用場面
**推奨:**
- ページロード時のアニメーション
- ホバー効果
- 定型的なトランジション

**非推奨:**
- インタラクティブなドラッグ操作
- リアルタイム物理シミュレーション

## 活用例

### ページエントランス
```css
.page-enter {
  animation: slideIn 0.8s linear(
    0, 0.01, 0.04, 0.09, 0.16, 0.25, 0.36, 0.49, 0.64, 0.81, 1
  );
}

@keyframes slideIn {
  from { transform: translateY(50px); opacity: 0; }
  to { transform: translateY(0); opacity: 1; }
}
```

### マイクロインタラクション
```css
.button:hover {
  animation: buttonPress 0.3s linear(
    0, 0.25, 0.5, 0.75, 1, 0.9, 0.8, 0.85, 0.95, 1
  );
}
```

## ブラウザサポート
- Chrome 113+
- Firefox 112+  
- Safari 16.4+
- **Baseline 2024**: 広く利用可能

## 学習リソース
- [Joshua Comeau - Animation Course](https://www.joshwcomeau.com/animation/)
- [Linear Easing Generator](https://linear-easing-generator.netlify.app/)
- [MDN: easing-function](https://developer.mozilla.org/en-US/docs/Web/CSS/easing-function)